name: plan
version: "1.0"
description: >
  HTN (Hierarchical Task Network) planner via IPyHOP.
  Exposes plan / replan / simulate over six built-in domains.

domains:

  simple_travel:
    tool: plan_simple_travel
    type: task-based
    entities:
      persons: [alice, bob]
      locations: [home_a, home_b, park, station, downtown]
      taxis: [taxi1, taxi2]
    state_variables:
      loc:  "dict[entity -> location] — current position of each person/taxi"
      cash: "dict[person -> float]    — cash on hand"
      owe:  "dict[person -> float]    — amount owed to taxi driver"
      rigid.types: "dict[type -> list] — type membership (person/location/taxi)"
      rigid.dist:  "dict[tuple(loc,loc) -> int] — distances between locations"
    actions:
      walk(p, x, y):       "walk person p from x to y; precondition: dist(x,y) <= 2"
      call_taxi(p, x):     "summon taxi1 to location x, person p boards"
      ride_taxi(p, y):     "ride taxi to y; sets owe[p] = 1.5 + 0.5*dist"
      pay_driver(p, y):    "pay owe[p] from cash[p], person arrives at y"
    tasks:
      travel(p, y): "get person p to location y by any available means"
    methods:
      do_nothing:       "no-op if p is already at y"
      travel_by_foot:   "walk if dist <= 2"
      travel_by_taxi:   "call+ride+pay if cash >= fare"

  blocks_world:
    tool: plan_blocks_world
    type: goal-based (MultiGoal)
    entities:
      blocks_problem_1: [a, b, c]
      blocks_problem_2: [a, b, c, d]
      blocks_problem_3: "integers 1..19"
    state_variables:
      pos:     "dict[block -> block|'table'] — what each block is resting on"
      clear:   "dict[block -> bool]          — whether the top surface is clear"
      holding: "dict['hand' -> block|False]  — what the hand is holding"
    actions:
      pickup(b):    "pick up block b from table; precondition: clear[b], not holding"
      unstack(b,c): "unstack b from c; precondition: pos[b]==c, clear[b], not holding"
      putdown(b):   "put held block b on table"
      stack(b,c):   "stack held block b on top of c; precondition: clear[c]"
    tasks:
      move_blocks(goal): "rearrange all blocks to satisfy MultiGoal"
      move_one(b, dest): "move one block b to destination"
      get(b):            "pick up or unstack block b into hand"
      put(b, dest):      "place block b onto dest (table or another block)"
    problems:
      "1a": "init_state_1 → goal1a (3 blocks, full goal: c on b on a)"
      "1b": "init_state_1 → goal1b (3 blocks, partial goal: c on b on a)"
      "2a": "init_state_2 → goal2a (4 blocks, full goal)"
      "2b": "init_state_2 → goal2b (4 blocks, partial goal)"
      "3":  "init_state_3 → goal3  (19 blocks, large rearrangement)"

  rescue:
    tool: plan_rescue
    type: task-based
    entities:
      wheeled_robots: [r1, w1]
      drones: [a1]
      persons: [p1]
    state_variables:
      loc:           "dict[entity -> (x,y)] — grid position"
      robot_type:    "dict[robot -> 'wheeled'|'uav']"
      has_medicine:  "dict[robot -> int]"
      status:        "dict[entity|loc -> 'free'|'injured'|'unk'|'clear']"
      altitude:      "dict[drone -> 'high'|'low']"
      current_image: "dict[drone -> image|None]"
      rigid.obstacles:     "set of (x,y) tuples"
      rigid.wheeled_robots: "tuple of wheeled robot ids"
      rigid.drones:         "tuple of drone ids"
    actions:
      move_euclidean(r,l,l_,dist): "move wheeled robot r from l to l_ (straight path)"
      move_manhattan(r,l,l_,dist): "move wheeled robot r (manhattan path)"
      move_curved(r,l,l_,dist):    "move wheeled robot r (curved path)"
      move_fly(r,l,l_):            "fly drone r from l to l_"
      change_altitude(r,alt):      "change drone r altitude to 'high' or 'low'"
      capture_image(r,camera,l):   "capture image at location l"
      inspect_person(r,p):         "inspect person p"
      support_person(r,p):         "administer medicine to person p"
      inspect_location(r,l):       "inspect location l for hazards"
      replenish_supplies(r):       "replenish medicine supplies for robot r"
      engage_robot(r):             "mark robot r as engaged/active"
      free_robot(r):               "release robot r back to free status"
      check_real(l):               "verify real status of location l"
    tasks:
      move(r, l):        "move robot r to location l"
      survey(r, l):      "survey location l using robot/drone r"
      rescue(r, p):      "rescue person p using robot r"
      help_person(r, p): "help injured person p"
      get_supplies(r):   "get medicine supplies for robot r"

  robosub:
    tool: plan_robosub
    type: task-based
    entities:
      robot: [r]
      torpedoes: [t1, t2]
      locations: [l0, l1, l2, l3, l4, l5]
      objects: [g, gm1, gm2, cm1, cm2, gp1, gp2, v1, v2, c1, ap1, ap2, d1, s1]
    state_variables:
      loc:              "dict[entity -> location] — current position"
      found:            "dict[entity|loc -> bool]  — whether object/zone is found"
      crossed_gate:     "dict[gate -> bool]"
      traversed_path:   "dict[guide_path -> bool]"
      vampire_touched:  "dict[vampire -> bool]"
      coffin_filled:    "dict[coffin -> list]      — garlic items dropped in"
      staked_dracula:   "dict[dracula -> list]     — torpedoes used"
      decapitated:      "dict[dracula -> bool]"
      surfaced:         "dict[robot -> bool]"
      rigid.adj:        "dict[loc -> list[loc]]    — adjacency graph"
      rigid.type:       "dict[entity -> type_str]  — object type labels"
    actions:
      search_for(loc):       "search zone to reveal adjacent objects"
      localize(obj):         "localize a specific object"
      localize_ap(ap):       "localize acoustic pinger ap"
      move(loc):             "move robot to adjacent location"
      cross_gate_40(gate):   "cross gate scoring 40 points"
      cross_gate_60(gate):   "cross gate scoring 60 points"
      pick(obj):             "pick up object"
      trace_guide_path(gp):  "follow guide path"
      touch_back_v(v):       "touch back of vampire"
      touch_front_v(v):      "touch front of vampire"
      open_c(coffin):        "open coffin"
      drop_garlic_open_coffin(gm,c):   "drop garlic into open coffin"
      drop_garlic_closed_coffin(gm,c): "drop garlic into closed coffin"
      decap_d(d):            "decapitate dracula"
      stake_decap_d(t,d):    "stake decapitated dracula with torpedo t"
      stake_norm_d(t,d):     "stake normal dracula with torpedo t"
      surface(cm,s):         "surface at surfacing zone s with coffin marker cm"
    tasks:
      pinger():           "locate acoustic pingers (ap1, ap2)"
      main(loc_list):     "complete all objectives in each zone in loc_list"
      move(loc):          "navigate to location"
      cross_gate(gate):   "cross gate"
      pick(obj):          "pick up object"
      trace_path(gp):     "trace guide path"
      slay_vampire(v):    "slay vampire"
      drop_garlic(gm,c):  "drop garlic in coffin"
      stake_heart(t,d):   "stake dracula's heart"
      surface(s):         "surface at zone"

  healthcare:
    tool: plan_healthcare
    type: task-based, temporal
    temporal: true
    origin_time: "2025-01-15T08:00:00Z"
    entities:
      rooms: [OR1, OR2, OR3]
      patients: [patient1, patient2, patient3]
      surgery_types: [cardiac, orthopedic]
    state_variables:
      room_status:          "dict[room -> 'available'|'cleaned'|'occupied']"
      room_equipment:       "dict[room -> surgery_type]"
      patient_location:     "dict[patient -> room]"
      patient_surgery_type: "dict[patient -> surgery_type]"
      surgery_complete:     "dict[patient -> bool]"
    actions:
      prepare_room(room, surgery_type): "prepare room; duration PT30M"
      perform_surgery(patient, room, surgery_type): "perform surgery; duration PT2H"
      recover_patient(patient, room):   "patient recovery; duration PT15M"
      clean_room(room):                 "clean room after surgery; duration PT20M"
    tasks:
      schedule_surgery(patient, room, surgery_type): "schedule full surgical workflow for patient"
    methods:
      schedule_surgery:        "full workflow: prepare → surgery → recover → clean"
      schedule_simple_surgery: "simplified workflow for pre-cleaned rooms"

  temporal_travel:
    tool: plan_temporal_travel
    type: task-based, temporal
    temporal: true
    origin_time: "2025-01-01T10:00:00Z"
    note: "same domain as simple_travel with ISO-8601 timestamps on every action"
    entities:
      persons: [alice, bob]
      locations: [home_a, home_b, park, station, downtown]
      taxis: [taxi1, taxi2]
    state_variables:
      loc:  "dict[entity -> location]"
      cash: "dict[person -> float]"
      owe:  "dict[person -> float]"
      rigid.types: "dict[type -> list]"
      rigid.dist:  "dict[tuple(loc,loc) -> int]"
    actions:
      walk(p, x, y):    "walk; duration PT5M per unit distance"
      call_taxi(p, x):  "call taxi; duration PT0S (instant)"
      ride_taxi(p, y):  "ride taxi; duration PT10M"
      pay_driver(p, y): "pay driver; duration PT0S (instant)"
    tasks:
      travel(p, y): "get person p to location y"
